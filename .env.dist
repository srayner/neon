# Database
DATABASE_URL=

# Server Configuration
SERVER_NAME=

# Optional: Base URL for API calls (only needed in production)
# NEXT_PUBLIC_BASE_URL=

# Agent Authentication (required for remote agents)
# Shared secret that agents use to register with master
AGENT_SECRET=
# Secret key for signing JWT tokens (generate a secure random string)
JWT_SECRET=

# Backups Browser
# Root directory for backup file browsing (optional)
BACKUP_ROOT=
